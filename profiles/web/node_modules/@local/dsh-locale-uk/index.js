// Host-side entry for the cordis loader. This plugin is client-only
// (dictionaries live in ./client.js and run in the browser via dsh.client),
// so the host registration is intentionally a no-op.
export function apply() {}
